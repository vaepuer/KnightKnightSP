using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WiggleMode : MonoBehaviour
{

    /*
    
    Add some kinda shader that pulsates when mouse is over object

    Check if Mouse is over object
    Check if Y = > 0.1
    If both return true:
        
        -   Start Wiggle Animation
        -   Stop pulsating shader animation, switch to solid shader
        -   Make wiggle <swish swish swish> sound effect with high-pitched struggle noises

    If both return false:

        - Do Nothing

     */
}
